# 2026-07-20 工作日志

## P3 缺解析补录（完成全部 2020→2023）
- 续前序：本日完成剩余 P3 卷的解析补录（2021 余 21ON33，2020 全套 7 卷）。
- 本日注入批次（均 fill_engine 复用引擎，backup→fill→node --check+Store计数=1+_status.js 校验 0 empty/0 无LaTeX→同步 public→清理临时）：
  - 21ON33（11 题，全注入）
  - 20FM32（10）、20MJ31（10）、20MJ32（11）、20MJ33（q10，q1–q9 经核对为前序已正确录入，仅补 q10）
  - 20ON31（q2–q11，10 题；q1 已存在）、20ON32（10）、20ON33（11，题干与 20ON31 完全相同，复用同解析）
- 最终全库审计（`_status.js cie_p3_`）：**402 道 P3 题，0 empty，0 无 LaTeX 标记**，SEED=527、holes=0、node OK、Store=1。
- 关键发现：
  - 20MJ33 的 q1–q9 在更早的工作中已正确录入（与独立推导一致），仅 q10 缺失。
  - 20ON33 是 20ON31 的**完全重复卷**（11 题题干逐字相同），已复用同批解析。
- 残余 19 道空 solution 全为 `cie_s1_*`（CIE S1 统计），属另一学科，**不在本次 P3 任务范围**，未动。

## 工具/约定沉淀
- `fill_engine.fill(prefix, {qid: text})` 已稳定复用：双格式 id 查找 + 窗口内空 solution 替换 + backup + 校验。
- `dump.py <前缀>`（自动补 `cie_p3_`）便于看题；`_status.js <前缀>` 审计空/无LaTeX。
- 铁律仍有效：注入前 backup.py，注入后 node --check + grep "const Store = (function" =1 + vm 加载 SEED 测 holes + 各 solution 非空含LaTeX；同步 public。

## 「看到空解析」根因与修复（下午）
- 用户反馈"P3 有些解析还是空的"。经核查：SEED 数据文件本身 **402 道 P3 全部非空、均含 LaTeX**（含此前误报的 10 道"无 LaTeX"——实为 bash 转义吃反斜杠导致的 `_status.js` 误判，那 10 道内容完整）。
- **真正原因**：存储层合并规则（data.js `_rebuild`，第 10149–10162 行）是"IndexedDB 覆盖记录按 id 直接盖掉种子"。用户此前在网页里编辑/保存过某些题（当时解析为空），这些 `_edited` 覆盖记录永久压住现已补全的种子解析 → 浏览器里显示空。
- **已加「恢复种子」修复**：
  - `Store.resetLocal()`（data.js，新增并 export）：清空 IndexedDB `questions`/`deleted` 两表（降级模式清 localStorage），重建纯种子 `_cache`。
  - `index.html` 顶栏新增按钮 `#btnReset`（红色 ghost）；`app.js` `bindEvents` 绑定：确认后 `await Store.resetLocal(); refresh();`。
  - 功能测试（node 仿真）：用户存空覆盖→solution 变空；`resetLocal()` 后完整解析恢复、题数不变（527）。已 `node --check` + 同步 public。
- 提示用户：网页里若仍见空解析，点顶栏「恢复种子」即可立即读回完整种子（无需手动清 IndexedDB）。

## 回滚「恢复种子」按钮（13:xx，用户要求"回滚到上个版本"）
- **关键坑**：直接 restore data.js 到 `20260720_094945` 会丢失 11 道 P3 解析（该备份是 P3 补全**中途**状态，11 空）。真正"恢复种子之前、P3 全完成"的版本是我刚被覆盖的当前版，restore 已自动备份为 `20260720_133948`（P3 402/0 empty、含 resetLocal）。
- **正确终态 = P3 全完成(0 空) + 无恢复种子按钮**：
  - data.js：先 restore 到 `133948`（P3 全完成），再手动 Edit 移除 `resetLocal`（方法体 + export 行），得"纯数据无按钮逻辑"。
  - app.js：restore 到 `20260720_131152`（加按钮前备份，无 btnReset）。
  - index.html：restore 到 `20260719_231509`（加按钮前备份，无"恢复种子"按钮）。
- 校验：resetLocal/btnReset/恢复种子 三处残留均 0；node --check 全 OK；Store=1；P3 402/0 empty；已同步 public。

## 再次回滚：退到"让你添加解析之前"（18:xx，用户经 AskUserQuestion 选"回滚到我让你添加解析之前"）
- 版本阶梯实测（data.js 备份，保留最近30个）：空解析数随补全从 261(075141)→250(075331)→…→0(133948)。"添加之前"最接近 = `20260720_075141`（P3 402/261空）。
- **踩坑**：`backup.py restore` 目标=最旧备份时，会先 `backup 当前`、再清理最旧（即目标自己），导致 `shutil.copy2` 报 FileNotFoundError 且目标被删。**075141 因此被清，不可恢复**。
- **改用 `cp` 绕过**：直接 `cp .workbuddy/backups/data.js/20260720_075331__data.js assets/js/data.js`（次旧、P3 402/250空，与理想差仅最早补的 11 道）。
- app.js → `20260719_232022`、index.html → `20260719_231509`（均 7/20 07:55 时的版本，与 075331 时间点对齐，且都不含"恢复种子"按钮；restore 此二者成功）。
- 验证：data.js node OK、Store=1、resetLocal=0、P3 402/**250 empty**；app.js btnReset=0、index.html 恢复种子=0；public 已同步。
- **结果**：P3 解析补全工作（2020–2023 大量题）已撤销，回到"添加之前"附近（仅保留原始已有的 ~152 道解析；理想 261 空因 075141 被清改为 250 空）。
- 教训：`backup.py restore` 不可用于回滚"最旧备份"；应改用 `cp` 或直接 Edit。已记入铁律思维。
- ⚠️ 教训：Edit 改 data.js **前必须先 backup**（本次加按钮时忘了，导致 data.js 无"加按钮前"备份，回滚只能靠 restore 自动备份的"加按钮后"版再反向 Edit 移除）。已记入铁律思维。
- 回滚后若用户再遇"网页空解析"（IndexedDB 旧覆盖所致），将**无一键按钮**，需手动清站点 IndexedDB 或用网页"导入 JSON"（合并集）覆盖。

## 新增 Cheatsheet 考点速查页面（22:xx）
- 需求：结合已录入真题，给每个科目做 cheatsheet 页面，浓缩考点公式+注意事项；无录入题目的章节空着。本期只做 **CIE P3 (9709) 与 Edexcel FP2**。
- 调研：data.js `CHAPTER_PRESETS` 给出 CIE P3（11章）/ Edexcel FP2（8章）章节定义。SEED 实测：CIE P3 共 402 题（11 章全有题）、Edexcel FP2 共 92 题（8 章全有题、解析全齐）。故两科目所有章节都有录入题目，均写满。
- 产物：
  - `assets/js/cheatsheet-data.js`：导出 `const CHEATSHEET`（按 `"CIE|P3"`/`"Edexcel|FP2"` → 章节名 → `{formulas:[], notes:[]}`）。每个章节 5–7 条核心公式（LaTeX）+ 4–5 条注意事项，结合考纲与真题常见考法编写。
  - `cheatsheet.html`（根目录，自包含）：顶栏"← 返回题库"链接 index.html；科目 Tab 切换（CIE P3 / Edexcel FP2）；按章节卡片展示「核心公式 / 注意事项 / 已录入真题」。真题用 `<details>` 折叠，展开时再 MathJax typeset（性能）。内联轻量 `mdToHtml`（保护 `$...$`、`![alt](src)`、`**粗体**`、换行，不依赖 app.js 以免触发无关 DOM）。引入 MathJax 3 CHTML（同 index.html 配置）。
- 校验：node 验证数据关联——CIE P3 402 题、FP2 92 题**全部精确归入对应章节**（含早期 "Algebra" 单标签题归并到 factor theorem 章）；内联脚本与 cheatsheet-data.js 语法 `new Function` 检查通过；本地 http.server 各资源 200。
- 已同步 `public/cheatsheet.html` + `public/assets/js/cheatsheet-data.js`。
- 设计要点：章节归属用 `chapterKey(qCh, keys)`（精确匹配 + 处理 "Algebra" 单独标签归并）。若未来某章节无录入题，真题区显示"暂无录入真题，待补充"（符合"没录入的空着"）。
