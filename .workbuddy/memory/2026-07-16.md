# 2026-07-16 工作日志

## 列表分页（每页 10 题）（00:08）
- 用户在 P0(IndexedDB 存储)+P1(虚拟滚动) 之后要求「分页，每10道题一页」（题目变多后浏览不便）。
- 实现：在 `assets/js/app.js` 给 `state` 加 `page:1`、`const PAGE_SIZE=10`；`renderList()` 改为先 `filteredQuestions()` 全量过滤，再 `slice((page-1)*10, page*10)` 取当前页 10 题喂给 `vlBuild`（复用 P1 虚拟滚动，单页内仍窗口化）；新增 `pageNumbers()`（>10 页时带省略号）+ `renderPager(total,pages)` 渲染页码/上下页/信息；`bindEvents` 加 `#pager` 事件委托（点击切换 `state.page` 后 `renderList`）；搜索/筛选/侧边栏切换时 `state.page=1` 复位。
- `index.html`：`.content` 内 `.list-wrap` 后加 `<div class="pager" id="pager">` 作为固定底栏（在滚动区外，不随列表滚动）；`style.css` 加 `.pager` 样式（`.pg-btn.active` 高亮当前页、`.pg-info` 显示「第 X / Y 页 · 每页 10 题 · 共 N 题」）。
- 验证：`node --check` app.js/data.js 通过；DOM 桩集成测试（合并 data.js+app.js 模拟浏览器加载，修正 IndexedDB 桩需返回带 `onsuccess` 的 request 对象）确认第1页实挂 10 卡（非全量139）、分页器显示「第 1 / 14 页」、点击页码2 切换到第2页（10 卡 / 「第 2 / 14 页」）→ PAGINATION_OK。改动前已 backup.py 备份 app.js/style.css/index.html（时间戳 20260716_000818）。
- 已知：组卷「全选」仍作用于全部筛选结果（跨页），非仅当前页；与分页前行为一致，未改。

## 解析展开被下方卡片遮挡（修复）（00:16）
- 症状：虚拟列表里卡片是 `position:absolute` 兄弟节点，按 DOM 顺序绘制；解析展开时下方卡片（N+1）因绝对定位层叠规则盖住上方卡片展开的解析。根因：① `typeset(sol)` 异步，同步 `vlMeasure` 测到的高度偏小、下方卡片 `top` 偏移没被推下；② 异步排版那段时间下方卡片会盖住解析；③ 绝对定位兄弟节点天然层叠遮挡。
- 修复（5 处，均在 `assets/js/app.js`）：① 解析展开时给该卡 `card.style.zIndex="30"` 置顶（收起时复位）；② `typeset(sol, cb)` 用回调，排版完成后再 `vlMeasure` 一次推正偏移；③ 给每张卡加 `ResizeObserver`（`vlMakeCard`），内容/公式/配图高度变化自动 `vlMeasure` 重排下方卡片（彻底消除时序差）；④ 回收/重建卡片时 `ro.disconnect()` 防泄漏与误测（`vlRender` 回收循环、`vlReset`、`vlUpdateItem` 旧节点）。
- 验证：`node --check` 通过；DOM 桩集成测试（模拟点击展开）确认 `z-index="30"` 置顶、`card0.offsetHeight` 100→500、`vidx1` 卡片 `top` 112px→512px（被推下）→ OCCLUSION_FIX_OK。改动前已 backup.py 备份 app.js（时间戳 20260716_001659）。
- 纯 CSS 层叠 + ResizeObserver 自动重测双保险；虚拟滚动逻辑未改。

## CIE P3 2025 ON Paper 35 录入（00:29）
- 用户提交 `F:/题库输入表格/CIE P3 录入/25_ON_35.xlsx`（11 道题，题干均为 `=DISPIMG` 嵌入截图，其余列空）。
- 流程：用 openpyxl(3.1.5) 读 Excel → zipfile 解包 `cellimages.xml`+rels 映射 DISPIMG ID→media → 提取 11 张 stem 截图到 `data/stem_sources/25_ON_35/` → 逐张 Read 视觉转录为 LaTeX 题干 → 分类章节（CIE P3 10 章）、定难度/分值、写解析 → 注入 data.js 的 `SEED_QUESTIONS` 数组。
- **11 道题完整清单**：
  | Q# | id | 章节 | 分值 | 难度 |
  |---|---|---|---|---|
  | 1 | cie_p3_25ON35_q1 | Algebra (modulus functions) | 4 (1+3) | 2 |
  | 2 | cie_p3_25ON35_q2 | Complex numbers | 4 | 3 |
  | 3 | cie_p3_25ON35_q3 | Logarithmic and exponential functions | 5 (2+3) | 2 |
  | 4 | cie_p3_25ON35_q4 | Logarithmic and exponential functions | 5 | 3 |
  | 5 | cie_p3_25ON35_q5 | Complex numbers | 5 | 3 |
  | 6 | cie_p3_25ON35_q6 | Trigonometry | 6 (4+2) | 3 |
  | 7 | cie_p3_25ON35_q7 | Integration | 8 (4+4) | 3 |
  | 8 |cie_p3_25ON35_q8 | Trigonometry | 9 (5+4) | 4 |
  | 9 | cie_p3_25ON35_q9 | Vectors | 10 (2+4+4) | 4 |
  | 10| cie_p3_25ON35_q10 | Numerical solution of equations | 10 (5+2+3) | 4 |
  | 11| cie_p3_25ON35_q11 | Differential equations | 9 (1+8) | 4 |
- 验证：`node --check` 通过；稀疏空洞检测 0；schema 完整性（id/examRef.label/difficulty/solution 全有）0 缺陷；总题数 150（139+11）；id 全唯一。备份：data.js（时间戳 20260716_003507）。
- ⚠️ 解析由 AI 撰写，建议教师核对数学准确性（尤其是 Q9 向量角度方程的二次解、Q8 R-formula 三角方程的周期解）。

## CIE P3 2025 MJ Paper 31 录入（10:54）
- 用户提交 `F:/题库输入表格/CIE P3 录入/25_MJ_31.xlsx`（11 道题，题干均为 DISPIMG 截图，Q11 另带一张配图），强调「注意题目顺序」。
- 同 25_ON_35 流程：openpyxl 读表 → zipfile 解 cellimages.xml+rels 映射 → 提取 12 张图（11 stem + Q11 配图）到 `data/stem_sources/25_MJ_31/` 严格按 cell/题序命名 → 逐张转 LaTeX → 分类章节/难度/分值/写解析 → 注入 SEED_QUESTIONS。
- ⚠️ **注入踩坑（重要教训）**：字符串拼接注入反复因末尾 `}];`（`}` 与 `];` 同行、无换行）被错误消费导致缺逗号/缺 `}`。**Python 的 `json.loads` 会误报**——现有 data.js 用 JS 语法（尾逗号、个别 chapter 为字符串），严格 JSON 解析器拒绝但 Node 运行时正常。**校验必须用 Node 运行时 eval，勿用 Python json.loads**。最终 v3「解析式定位末条目 `}` + 保留 + 插入」注入成功。
- **11 题清单（顺序正确 Q1→Q11，source `25_MJ_31_1`..`_11`）**：
  | Q# | id | 章节 | 分值 |
  |---|---|---|---|
  | 1 | cie_p3_25MJ31_q1 | Algebra (modulus functions) | 3 |
  | 2 | cie_p3_25MJ31_q2 | Logarithmic and exponential functions | 3 |
  | 3 | cie_p3_25MJ31_q3 | Complex numbers | 6 |
  | 4 | cie_p3_25MJ31_q4 | Trigonometry | 6 |
  | 5 | cie_p3_25MJ31_q5 | Algebra (factor/remainder theorem) | 5 |
  | 6 | cie_p3_25MJ31_q6 | Complex numbers | 6 |
  | 7 | cie_p3_25MJ31_q7 | Trigonometry (R-formula) | 8 |
  | 8 | cie_p3_25MJ31_q8 | Vectors | 9 |
  | 9 | cie_p3_25MJ31_q9 | Numerical solution of equations | 10 |
  | 10 | cie_p3_25MJ31_q10 | Differential equations | 8 |
  | 11 | cie_p3_25MJ31_q11 | Integration | 11 |
- 验证（Node 运行时）：总题数 150→**161**；新 11 题全在；空洞 0 / 重复 0 / 缺字段 0 / examRef.label 全有 / 11 题 chapter 均数组（仅最早 2 道示例题 seed_cie_p1_001、seed_cie_s1_001 chapter 为字符串，历史遗留，无关本次）。备份 data.js 20260716_102014。临时脚本已清理。
- ⚠️ 解析 AI 撰写，建议核对（尤其 Q9 数值解/积分、Q8 向量、Q7 R-formula 周期解）。浏览器生效需「导入 JSON」或清 IndexedDB 后刷新。

## 🔴 严重故障：root 版「网页无法加载」根因与修复（11:12）
- **症状**：present_files 打开 `E:/workbuddy/题库软件/index.html`（root 版），白屏/无法加载。
- **根因**：项目存在**两套并行代码**——
  - `assets/js/`（root，本会话一直在改）：161 题（含 25_ON_35/25_MJ_31）、app.js 有分页+遮挡，但 data.js **缺 `Store` 定义** → app.js 启动 `Store.init()` 抛 ReferenceError 白屏。
  - `public/assets/js/`（旧可运行构建）：data.js 有 `Store`（localStorage 版，139 题、旧 `topics` schema）、app.js 无分页/遮挡。
  - 用户此前能用分页/遮挡，说明一直开的是 root/index.html（当时有 Store）。**25_MJ_31 的 v3 注入脚本整文件重写 data.js 时，把 `];` 之后的 `Store`（IndexedDB 版，含 `init`）整段误删** → root 版从此白屏。
- **修复**：从注入前备份 `20260716_102014__data.js`（4146–4343 行的 IndexedDB 版 Store，含 `init`）精确提取补回 root/data.js 末尾。修复前已备份 `20260716_111207`。
- **验证（VM + localStorage 兜底 shim）**：`typeof Store==='object'`、`Store.init` 是函数、`Store.all()` 返回 161 题（含 25_MJ_31 11 题）、0 条缺 id/stem/chapter；`node --check` 通过。
- **铁律补充（防再犯）**：① 注入脚本**绝不能整文件重写 data.js**，必须只做局部插入（在 `];` 前插入新条目），或先备份、注入后确认 `const Store` 仍在；② 注入/改动后**必须用 VM/浏览器实际加载**确认 `Store` 与 `Store.init` 仍在、页面能渲染，而非只看 `node --check`；③ Python `json.loads` 对含尾逗号/非数组 chapter 的 JS 数据会误报，校验须用 Node 运行时 eval。
- **待办（非紧急）**：root 与 public 严重分叉（schema、题数、功能都不同）。当前 root 已修复且功能最全，建议统一以 root 为准，或将 public 同步为新版；否则用户易混淆。

## 🔴 故障：点「编辑」打不开编辑界面（12:34）
- **症状**：网页能加载、列表正常，但卡片「✏️ 编辑」点击后无反应（编辑弹窗不出现）。
- **根因**：与上次白屏同源——v3 整文件重写不仅丢了 `Store`，还把 `SEED_QUESTIONS` 数组闭合 `];` 之后、`Store` 之前的一段**辅助函数**一并丢失：`const _MONTHS`、`function _normMonth`、`function parseExamRef`（在备份 `20260716_102014__data.js` 的 4081–4145 行）。`openEdit` 走到 `updateSrcHint()`（app.js:828）调用 `parseExamRef` → `ReferenceError` 抛错，永远走不到 `openOverlay("#editOverlay")` → 界面不弹出。
- **修复**：从同一备份精确提取 4081–4145 行（`_MONTHS`+`_normMonth`+`parseExamRef`+注释）插入到 `const Store`（当前 4449 行）之前。修复前已备份 `20260716_123804`。
- **验证（Node VM + localStorage shim）**：`node --check` 通过；`typeof parseExamRef==='function'`、`parseExamRef("25_MJ_35_1")` 正确解析出 `2025 May/June · Paper 35 Q1`；`Store.all()` 仍 172 题；data.js 顶层符号与备份逐项一致（TAXONOMY/CHAPTER_PRESETS/SEED_QUESTIONS/_MONTHS/_normMonth/parseExamRef/Store），无遗漏。
- **铁律再补（防再犯）**：整文件重写 data.js 的代价不止丢 `Store`，还会丢数组之后的**所有**辅助声明。今后凡改 data.js 数组，**必须只做局部插入**、绝不整文件重写；改动后用 Node VM 实际加载并断言 `parseExamRef`、`_MONTHS`、`Store.init` 等仍在、且能调用 `Store.all()`，再交付。

## 安装 MinerU Document Extractor skill（11:22）
- 用户从附件（SKILL.md + _meta.json）要求安装该 skill。来源：MinerU 文档提取（OpenDataLab 官方 `mineru-open-api` CLI），把 PDF/Word/PPT/Excel/图片/网页转 Markdown/LaTeX/DOCX 等。
- 安全审计：仅 SKILL.md + _meta.json，**无**脚本/可执行/混淆代码；`allowed-tools` 限 `Bash(mineru-open-api:*)`；P1 项＝文档内容发往 mineru.net 服务端（metadata 已声明不留存）+ 需装外部 npm 包。结论 P2（安全），已向用户披露隐私点。
- 安装：`~/.workbuddy/skills/mineru-document-extractor-0.1.29/`（SKILL.md + _meta.json 已从 TEMP 提取包拷贝）。
- CLI 依赖 `mineru-open-api` 经**托管 node 22.22.2 的 npm** 装到 `~/.workbuddy/binaries/node/versions/22.22.2/`（隔离在托管环境、已在 PATH），验证 `version v0.5.9` 可用。flash-extract 免 token（限 10MB/20页），extract/crawl 需 `mineru-open-api auth` 配 MINERU_TOKEN。
- 用户随后提供 MinerU API token，已写入 `~/.workbuddy` 同级用户目录 `~/.mineru/config.yaml`（`token:` 字段；交互式 `auth` 只读 TTY 无法管道，故直接写 config.yaml）。`auth --show` 来源=config、`auth --verify`「Token format is valid」→ extract/crawl 高精度模式可用。token 值未写入任何 memory/日志（仅存于 config.yaml）。

## MinerU 实战：9709_s25_qp_35.pdf 题干提取（11:28–12:02）
- 源：`E:/workbuddy/题目拆分/9709_s25_qp_35.pdf`（~0.1MB，CIE 9709/35 P3 Pure Math 3, May/June 2025，11 题）。
- **踩坑**：① Go 二进制只认 Windows 路径（`E:/...`），Git Bash 的 `/e/...` 会被报「找不到路径」；② **`flash-extract` 在本沙箱必超时**（服务端 5min 默认上限，任务 expire 不可找回）；③ 改用 token-based `extract --model pipeline --language en -f md --timeout 1800`，后台跑约 15min 成功，输出到 `E:/workbuddy/题目拆分/9709_s25_qp_35_mineru/`（含 `9709_s25_qp_35.md` + `images/`）。
- **教训**：本环境用 MinerU 一律走 `extract`（token）而非 `flash`；路径用 `E:/` 形式。
- 二次整理：从全量 md 剥离试卷抬头/INSTRUCTIONS/INFORMATION/条码/页脚，归一 LaTeX 空格，产出纯题干 `E:/workbuddy/题目拆分/9709_s25_qp_35_题目.md`（11 题，含各 part 与 [分值]）。图示略去（保留在 images/，已在文件头说明）。
- ⚠️ OCR 个别分值未捕获（如 Q3(a)/Q4/Q9(a)/Q10(a)(b)/Q11），公式整体准确；建议教师核对数学与缺失分值。

## 题干 → 批量录入表转换（12:02–12:10）
- 模板：`E:/workbuddy/题库软件/批量录入模板_CIE_P3.xlsx`（sheet `CIE P3 录入`：考试局/科目,来源,题干,配图,章节,考点,难度,分值,解析；下拉＝Chapters!A1:A10、难度 1-5、考试局锁 CIE/P3；另有 `说明` sheet）。
- 产出：`E:/workbuddy/题库软件/批量录入_CIE_P3_9709_s25_35.xlsx`（基于模板，覆盖示例行，写满 11 题）。
- 列映射要点：① 来源格式 `YY_SERIES_VARIANT[_Q]` → `25_MJ_35_<Q>`（s25=May/June=MJ；变体 35 取自文件名 qp_35，**注意** CIE 9709 标准 P3 变体是 31/32/33，若实际为 33 需改）；② 题干=英文原版 LaTeX，小问小分内联 `\hfill (N)`（不包进 `$...$`）；③ 章节按 10 章最佳匹配（Q4 驻点→Logarithmic and exponential；Q6 参数方程→Trigonometry；Q7 arctan/积分→Integration）；④ 配图=空（Q5 有图但仅扫描件，待重绘 TikZ）；⑤ 解析=空（题卷无答案，待补）。
- ⚠️ 分值：Q1/Q2/Q5/Q6 用 OCR 实值（4/5/6/9）；Q3/Q4/Q7(a)/Q8(b)(c)/Q9(a)/Q10(a)(b)/Q11 为**估算**（合计 81，略高于真实 75，待按 mark scheme 校正）。
- 技术：openpyxl 经托管 venv（`~/.workbuddy/binaries/python/envs/default`，已 `pip install openpyxl`）；脚本 `E:/workbuddy/题目拆分/gen_entry.py`。heredoc 被沙箱误判 csc.exe 拦截，改用 Write 脚本文件再执行。

## CIE P3 2025 MJ Paper 35 批量录入（结构化模板，12:15）
- 用户提交 `E:/workbuddy/题库软件/批量录入_CIE_P3_9709_s25_35.xlsx`（即上面模板填好的 11 题，来源 `25_MJ_35_1..11`）。
- **与之前两批不同**：本题干是**直接写在单元格里的 LaTeX**（不是 DISPIMG 截图），仅 Q5 有配图（`=DISPIMG` → 提取 `xl/media/image1.png` 存 `data/images/25_MJ_35_Q5.png`，16KB 有效 PNG）；解析列空 → **AI 撰写 11 题解析**。
- **安全录入流程（已固化为防坑组合）**：
  1. openpyxl 读 `CIE P3 录入` 9 列 → Python 把 stems/metadata 先 dump 成 `data/_batch_25mj35.json`（**解析用 raw string `r"""..."""` 写，LaTeX 反斜杠原样保留**，再 json.dump 正确转义）。
  2. Node 读取该 JSON，做**只插入** `SEED_QUESTIONS` 数组（在数组闭合 `]` 前插入，绝不整文件重写），用**字符串感知的括号匹配器**（扫描时跳过 `"..."` 字符串内的 `[`/`]`，否则会被 `\\left[`/`\\right]` 等 LaTeX 骗到错误位置、把已有条目拦腰截断 → 语法错）——`subMarks` 由 stem 内 `\hfill (N)` 解析、createdAt 用 `Date.now()`。
  3. 注入前先 `backup.py backup data.js`；注入后 `node --check` + Node 运行时 eval 校验：必须确认 `const Store` 仍在、无空洞/重复/缺字段、新 11 题顺序正确。
- **验证（Node 运行时）**：总题数 161→**172**；新 11 题全在（id `cie_p3_25MJ35_q1..q11`，顺序 Q1→Q11）；空洞 0 / 重复 0 / 缺字段 0（仅最早 2 示例题 chapter 为字符串，历史遗留）；`Store` 在位；Q5 figure 路径解析到实际文件；`subMarks` 求和均等于 marks。备份 `20260716_122758`。临时脚本/中间 JSON 已清理。
- ⚠️ 解析 AI 撰写，建议核对（尤其 Q6 参数方程求导 $A=1/2$ 与法线 $y=-\sqrt2x+5$、Q8 迭代收敛 0.992、Q10 向量菱形 $b=-1/3$、Q11 微分方程解 $y\approx-0.331$）。浏览器生效需「导入 JSON」或清 IndexedDB 后刷新。

## 根据 Mark Scheme 校正批量录入表（12:56–13:12）
- 用户提交 `E:/workbuddy/题目拆分/9709_s25_ms_35.pdf`（MS），要求：补配图、题干分行、小问序号不加粗、按 MS 改分值并补充解析。
- 提取 MS：token-based `extract --model pipeline --language en -f md`，31 秒完成，输出到 `E:/workbuddy/题目拆分/9709_s25_ms_35_mineru/`。
- 从 MS 表格提取各小分（用正则抓 `<td ...></td><td ...>N</td><td ...></td></tr>` 总分行），Q1–Q11 合计 **75**（修正此前估算的 81）。主要修正：Q3(a) 2→非 4；Q4 5；Q7(a) 3；Q8(b)(c) 各 2；Q9(a) 5；Q10(a) 3、(b) 2、(c) 3；Q11 8。Q10 总分由 11 修正为 8。
- 配图：Q5 图已存在 `data/images/25_MJ_35_Q5.png`（与 MinerU 提取的 circle-locus 图一致），在 配图 列填 `data/images/25_MJ_35_Q5.png`，其余留空。
- 题干格式：去掉 `**(a)**` 加粗，改用 `(a)`；主语与 (a)/(b) 之间、方程块前后插入真实换行符（`\n`）。
- 解析：按 MS 写简洁中英混合解法，含关键步骤与最终答案。
- 产出：`E:/workbuddy/题库软件/批量录入_CIE_P3_9709_s25_35_已配分解.xlsx`（旧文件 `批量录入_CIE_P3_9709_s25_35.xlsx` 被占用/锁定，故用新文件名）。
- 工具：openpyxl（已装于托管 venv），临时脚本已清理。建议：关闭旧文件后可用新文件覆盖，或在网页「批量录入」中直接导入新 xlsx。

## 构建 skill：cie-paper-to-entry（13:12–13:30）
- 用户要求把「MinerU 拆卷子+MS→录入 Excel」整套流程封装成 skill。已建**用户级** skill：`~/.workbuddy/skills/cie-paper-to-entry/`（SKILL.md + _meta.json + scripts/build_entry.py）。
- 工作流（skill 固化）：`mineru-open-api extract`（卷子+ms 各一次，pipeline/en/md/--timeout 1800，后台跑防超时）→ 脚本 `split` 解析卷子 md 切题+关联配图、解析 ms md 取分值/解法 → 脚本 `build` 套 `批量录入模板_CIE_P3.xlsx` 生成录入表（可选 `--figures-dest` 把关联图复制为 `<来源>.png` 进 `data/images`）。
- 脚本关键处理（已对 9709_s25_qp_35 实测，11 题合计=75 全对）：
  - 切题兼容 OCR 把 `10` 写成 `1 0`、题号在 LaTeX `$7(\mathrm a)$`、题号缺口自动填；
  - 清条码/页眉/加粗，LaTeX 去空格，`[n]`→`\hfill (n)`，主句与小问间真实换行；
  - 配图仅关联「题干显式描述所给图」(the diagram shows/argand/locus)，否则留空并 notes 列出块内图；
  - 分值取自 ms 各 part 总分行（兼容 2/3/4 列表格、Alternative Method 行、LaTeX 题号），无总分行退回 mark 码合计并标「估」；
  - 章节关键词启发式，默认回退模板 Chapters 表（内置 CIE_P3_CHAPTERS 兜底）。
- 依赖：mineru-open-api（托管 node，token 在 ~/.mineru/config.yaml）+ 托管 venv 的 openpyxl/beautifulsoup4/lxml。
- 验证：生成的 `批量录入_CIE_P3_9709_s25_35_auto.xlsx` 11 题全对（分值/章节/配图 Q3+Q5/解析），模板 3 个数据验证保留。该 xlsx 为 skill 自动化产出的演示件（非人工精修版；精修版见 `..._已配分解.xlsx`）。
- 用户后续给新卷子时，直接调用此 skill 即可（输入卷子 PDF + ms PDF）。

## 修复：skill 题干残留条码/页码（转义星号）（13:35）
- 用户反馈生成的录入表题干里出现 `\* 0000800000009 \* 9` 这串码。根因：MinerU 把页脚条码星号输出为**转义** `\*`（独占一行 `\* 0000800000009 \*`），其后另有独占一行的裸页码（如 `9`）。而 `build_entry.py` 的 `is_barcode` 正则以裸 `*` 开头、`clean_text_line` 也只认裸 `*`，全被反斜杠挡住 → 条码行+页码行被当正文拼进题干（全卷系统性，每题都中招，如 Q8 出现 `\* 0000800000013 \* 13`）。
- 修复（`~/.workbuddy/skills/cie-paper-to-entry/scripts/build_entry.py`，改前已 .bak 备份）：
  1. `is_barcode`：正则改 `^\\?\*\s*\d[\d\s]*\\?\*?\s*$`（兼容转义星号）；新增独占一行 `^\d{1,3}$` 裸页码识别。
  2. `clean_text_line` 行内兜底：`re.sub(r'\\?\*\s*\d[\d\s]*\\?\*\s*\d*', '', line)`（吃掉转义条码 + 其后紧跟页码）。
- 验证：重跑 split→build，11 题条码残留 0、合计仍 75 分、3 个数据验证保留。产出 `E:/workbuddy/题库软件/批量录入_CIE_P3_9709_s25_35_clean.xlsx`（参数方程题为 Q6，题干已干净）。SKILL.md 清洗说明同步更新。

## 修复：卷末 Additional page 版权声明混入题干（13:38）
- 用户指出 Q11 题干尾部混入整段页脚声明（`## Additional page` + Cambridge/UCLES 版权声明，md 第 225 行起到 EOF，正好是 Q11 块尾部 → 被整段收进题干）。
- 修复（`build_entry.py`，改前已有 .bak）：新增 `is_boilerplate(line)`（匹配 additional page / if you use the following page to complete the answer / permission to reproduce items / every reasonable effort ... publisher / university of cambridge local examinations syndicate / (ucles) / www.cambridgeinternational.org / cambridge assessment international education is part of），并在 `extract_stem` 的「配图收集」与「文本行收集」两循环遇该起始行即 `break` 截断。
- 验证：重跑 split→build，产出 `批量录入_CIE_P3_9709_s25_35_clean2.xlsx`：11 题、版权声明残留 0、条码残留 0、合计 75 分、3 验证保留；Q11 题干干净收尾于「find the value of y when x = 2.」。SKILL.md 清洗说明同步加入页级声明说明。中间草稿 `题目拆分/draft_fix.json`、`draft_fix2.json` 已清理。

## CIE P3 2025 FM Paper 32 录入（13:46）— 解析留空（新规则）
- 用户提交 `25_FM_32.xlsx`，**新增要求：解析留空**（以后录入均不写 solution）。
- 模板：11 行，题干均为 DISPIMG 截图；章节/难度/分值/解析列全空 → 我根据截图内容填分类与元数据。
- ⚠️ **图片映射踩坑**：media 文件名字母序 ≠ cellimages 名称顺序。必须用 **rels 关系文件**做正确映射，否则全部错位。
- ⚠️ **括号扫描器 bug**：depth 必须初始化为 **1**（起始点在 `[` 之后）；末条目无尾逗号（`}\n];` 格式），搜索目标需适配。
- 注入：Node 脚本只插入数组（最后一个 `}` 后插入新条目），保留 Store/parseExamRef 不动。`node --check` 通过；11 个 25FM32 ID / Store / parseExamRef 全在。
- 总题数：172 → **183**。备份 `20260716_135857`。

## CIE P3 2024 ON Paper 31 录入（22:44）— 三项修正已应用
- 用户提交 `F:/题库输入表格/CIE P3 录入/24_ON_31.xlsx`（10 道题，模板格式，题干均为 DISPIMG 截图；Q6/Q10 另带配图）。
- **本次录入应用三项修正**（此前 FM32 的已知问题）：
  1. **换行符**：json.dumps 生成 stem → JS 源为 `\n`（运行时真实换行，mdToHtml 正确分段）
  2. **小问序号不加粗**：`(a)(b)(c)(d)` 纯文本
  3. **来源映射**：ON → month="Oct/Nav"，label 含 "2024 Oct/Nov"
- 流程：openpyxl → zipfile 解 cellimages 映射 → 提取 12 图(10stem+2fig) → Read 视觉转 LaTeX → json.dumps → **行式注入**(`];` 定位，补逗号，插条目)
- 注入技术：花括号搜索不可靠(LaTeX嵌套) → 行式定位 `];`；`node --check` 门禁；注入前 backup.py 备份。
- 10 题：Q1 因式定理(5)/Q2 积分(5)/Q3 隐函数求导(4)/Q4 三角恒等+方程(8)/Q5 迭代法(7)/Q6 三角积分面积+图(8)/Q7 部分分式+二项(9)/Q8 复数轨迹(9)/Q9 向量(10)/Q10 微分方程水箱(10)；总分 85；配图 Q6+Q10。
- 验证：node --check OK；总题 183→193；全 10 项 month/label/bold/fig/solution 正确。备份 20260716_225312。
