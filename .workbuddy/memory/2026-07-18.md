# 2026-07-18 工作日志

## 录入 22_ON_33（CIE P3 2022 Oct/Nov Paper 33，11 题）
- 文件 `F:/题库输入表格/CIE P3 录入/22_ON_33.xlsx`，新 9 列模板（考试局/科目/来源/题干/配图/章节/考点/难度/分值/解析），实际仅填 来源/题干/配图 三列，章节/考点/难度/分值/解析 全空。
- 配图列为数字占位（4/5/5/5/6/6/8/8/9/9/10），xlsx 未嵌入任何图片（无 media、drawing 无 blip），目录与 `data/images` 均无对应图 → 全部 `figure` 留空。
- 章节按题干内容推断（空列 → 字典）：Q1 ln 方程→`Logarithmic and exponential functions`；Q2 二项式展开→`Algebra (partial fractions and binomial expansions)`；Q3 积分→`Integration`；Q4 参数方程 dy/dx→`Differentiation`（纯微分法单标签）；Q5/Q6 复数→`Complex numbers`；Q7 R cos→`Trigonometry`；Q8 驻点+迭代→`[Differentiation, Numerical solution of equations]`（迭代定位驻点双标签）；Q9 向量→`Vectors`；Q10 微分方程→`Differential equations`；Q11 部分分式+积分→`[Algebra (partial fractions and binomial expansions), Integration]`。
- 难度/分值列空：难度给合理值（3/3/3/4/4/4/4/5/4/4/4）；marks 由题干 `\hfill(N)` 求和（4/5/5/5/6/6/8/8/9/9/10）。
- 注入脚本 `.workbuddy/inject_22on33.py`（正则+html.unescape 解析内联字符串，支持「优先读列、空则推断」，可复用于同批 22 系列新模板文件）。
- 校验全过：node --check OK；`grep -c "const Store = (function"`=1；cie_p3 194→205；11 题无缺失/无重复/无空洞；Store 段完整。
- commit `34d3e09` 推送 main。MacBook 可 `git pull` 同步。

## 录入 22_ON_32（CIE P3 2022 Oct/Nov Paper 32，10 题，含 2 图）
- 文件 `F:/题库输入表格/CIE P3 录入/22_ON_32.xlsx`，同 9 列模板，含 sharedStrings + 2 张 WPS DISPIMG 嵌入图（xl/media/image1.png、image2.png）。
- 解析列/配图列数字（'9'/'4'/'6'/'8'/'10'）为无意义占位 → solution 留空；仅 Q8/Q9 的 DISPIMG（`_xlfn.DISPIMG("ID_...",1)`）为真实图，已提取存 `data/images/22_ON_32_8.png`、`22_ON_32_9.png`。
- WPS cellImages 映射：`xdr:cNvPr name="ID_..."`（=worksheet 公式 ID）+ `a:blip r:embed` → rels → media/imageN.png。注入脚本 `.workbuddy/inject_22on32.py`（支持 sharedStrings + 动态 DISPIMG 映射）。
- 章节推断：Q3 驻点 x 坐标→`Differentiation`；Q8 曲线面积积分→`Integration`；Q9 半圆面积导出 θ 方程+迭代→`Numerical solution of equations`（非驻点，不触发 [Diff,Numerical]）；Q10 部分分式+积分→`[Algebra (partial fractions and binomial expansions), Integration]`。
- 校验全过：node --check OK；Store 计数=1；cie_p3 205→215；10 题无缺失/无重复/无空洞；Store 段完整；stem 无实体残留。

## 录入 22_ON_31（CIE P3 2022 Oct/Nov Paper 31，11 题，含 2 图）
- 文件 `F:/题库输入表格/CIE P3 录入/22_ON_31.xlsx`，同 9 列模板 + sharedStrings + 2 张 WPS DISPIMG 嵌入图（image1→Q9 曲线图、image2→Q11 向量固体图）。
- 解析列全为数字占位 `9`（Q11 空）→ solution 全部留空；章节列 Q9='10' Q11='11' 为占位 → 推断。
- WPS 图片映射兼容两种结构：`<etc:cellImage>` 与 `<xdr:pic>` 均按 `name="ID_..."`+`r:embed`→rels→media 提取（22_ON_31 为 `<xdr:pic>` 结构，与 22_ON_32 的 `<etc:cellImage>` 不同，通用正则已覆盖）。
- 章节推断（应用 Differentiation 规则）：Q1 模函数图+含绝对值不等式→`Algebra (modulus functions)`；Q2/Q5 复数→`Complex numbers`；Q3 ln 指数方程→`Logarithmic and exponential functions`；Q4/Q6 三角→`Trigonometry`；Q7 由梯度=12 导驻点方程+迭代定位→`[Differentiation, Numerical solution of equations]`（双标签）；Q8 微分方程→`Differential equations`；Q9 求最小点+面积→`[Differentiation, Integration]`（双标签）；Q10 部分分式+二项展开→`Algebra (partial fractions and binomial expansions)`；Q11 向量→`Vectors`。
- 校验全过：node --check OK；Store 计数=1；cie_p3 215→226；11 题无缺失/无重复/无空洞；Store 段完整；stem 无实体残留；2 图已存 `data/images/22_ON_31_9.png`、`22_ON_31_11.png`。
- commit 待推送（紧随本次录入）。

## 备注
- 录入目录中仍有 22 系列其他卷（22_FM_32 / 22_MJ_32 / 22_MJ_33 等，修改时间 07-17/18）及 25 系列（在「已录入」子目录）尚未处理，按用户后续提交逐个录入。
