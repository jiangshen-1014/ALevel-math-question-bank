# 2026-07-17 工作日志

## 上线/登录系统方案讨论（00:00–00:10）
- 用户问如何上线 + 加登录 + 让其他主机访问并录入。诊断：当前纯静态前端 + 浏览器本地 IndexedDB，无后端/登录/共享写入，必须加后端+共享库+登录才能多人共享。
- 给出架构方案：Node/Express + 共享库 + JWT 登录 + 前端 Store 改调 API + 角色(editor/viewer)。
- 经多轮澄清，用户决策：**暂时搁置上线，优先完善本地数据**。原因：介意标准 CVM ¥65/月成本，倾向免费/极便宜方案。
- 已搭未完成的 `server/` 脚手架（package.json + db.js + 待补 auth.js）封存，本地应用不加载、不影响使用。
- 记录到 MEMORY.md「上线/部署倾向」：候选 Lighthouse 特价 / CloudBase 云开发 / Supabase；排除纯静态托管与标准 CVM。

## 用户将在另一台 MacBook 上继续本项目（12:37）
- 项目纯静态(无构建)，MacBook 上 `python3 -m http.server 8000` 或 `npx serve` 即可跑；必须用 http 不能 file:// (IndexedDB/CDN 不稳)。
- 数据双层：种子题在 `assets/js/data.js`(拷文件夹即带走)；网页 UI 改录存浏览器 IndexedDB(不随文件夹走，需导出 JSON 再导入)。
- `public/` 是陈旧副本，真身是根 `index.html`+`assets/`。
- 录入两种方式：① 把 xlsx 拿给本会话，我注入 data.js 再同步；② MacBook 自建 venv+openpyxl 复刻流程。
- 长期同步建议用 Git(本机 git init→推 GitHub/Gitee→MacBook clone)。待用户确认是否现在初始化。

## 项目已上传 GitHub（12:48）
- 本地 `git init -b main` + 首提交 `8f8aea7`(188 文件, data.js 193 题种子等)；`.gitignore` 忽略 `.workbuddy/backups/` 与陈旧 `public/`。
- 生成 SSH key(`~/.ssh/id_ed25519`, 备注 mathbank-sync) 并加入 GitHub 账号。
- remote: `git@github.com:jiangshen-1014/ALevel-math-question-bank.git`，已 `push -u origin main` 成功。
- MacBook 端：`git clone git@github.com:jiangshen-1014/ALevel-math-question-bank.git` → `python3 -m http.server 8000` → 开 http://localhost:8000。
- 后续录入批次提交后 `git push`；MacBook 端 `git pull` 同步。Windows 浏览器 UI 改录仍在 IndexedDB，需导出JSON导入才跨机。

## CIE P3 2024 ON Paper 32 录入（13:00）— 11 题
- 用户提交 `F:/题库输入表格/CIE P3 录入/24_ON_32.xlsx`（11 道题，模板格式，题干 DISPIMG 截图；xlsx 中配图错位在 Q6 行实际属 Q3）。
- 流程：openpyxl+zipfile 解 cellimages → 提取 12 图(11stem+1fig) → Read 视觉转 LaTeX → **raw string 三引号**避免转义问题 → 行式注入 `];` 前。
- 注入技术改进：用 `r"""..."""` raw string 写 stem（解决之前 heredoc 内 `$` 与 `\` 转义冲突导致的 SyntaxError）；条目间逗号处理 `[l+",\n" for l in lines[:-1]] + [lines[-1]+"\n"]`。
- 配图修正：xlsx 将 Q3(ln y 直线图)的 fig 放在 Q6 行 → 检查后重命名文件 `_6→_3` + 修正 data.js figure 字段。
- 11 题：Q1 二项展开(4)/Q2 指数方程(3)/Q3 对数线性化+图(4)/Q4 三角根+迭代(3)/Q5 复数平方根(5)/Q6 复数De Moivre(4)/Q7 三角方程(6)/Q8 参数法求法线(8)/Q9 向量梯形(11)/Q10 微分方程气球(13)/Q11 积分换元PF(14)；总分 85；配图仅 Q3。
- 验证：node --check OK；总题 193→204；全 11 项 month/label/bold/fig/solution 正确。
- Git commit `1144233` 已 push 到 GitHub（MacBook 可 git pull 同步）。备份 20260717_130247。

## 修复白屏：data.js 丢失 Store IIFE（13:19）
- 现象：24_ON_32 录入并提交后，网页白屏。根因：注入脚本把文件截断在 SEED_QUESTIONS 的 `];` 处，导致其后完整的 `Store` IIFE（含 init/all/upsert/remove/replaceAll、parseExamRef、_MONTHS、_normMonth）丢失；app.js 调 `Store.all()` 找不到 → 白屏。题目数据(204题)完好。
- 修复：从注入前备份 `20260717_130247` 提取 `];` 之后 268 行 Store 段落，Python 追加到当前文件(校验末尾确为 `];` 才追加)。`node --check` OK；python 解析 SEED_QUESTIONS = 204 题；Store/parseExamRef/_MONTHS 均在。
- 提交 `7b0d6f2` 已 push。
- ⚠️ **铁律升级**：录入注入必须用「行式插入到 `];` 之前、保留 `];` 及之后全部内容」的方式，绝不可整体重写或截断 `];` 之后。事后务必 `node --check` + grep 确认 `const Store = (function` 仍存在。

## 用户修正录入换行约定（20:52）
- 用户明确：题干内字面 `\newline` 和 `\n\n` **都应删除**；Excel 中换行直接用 **Alt+Enter**（真实换行符），不要写字面转义。已同步更新 MEMORY.md「换行符」铁律。此前 22_MJ_31 把字面 `\newline` 转成了真实 `\n`（效果等同 Alt+Enter，视觉正确，无需回改）。

## CIE P3 2024 FM Paper 32 录入（21:xx）— 11 题
- 用户提交 `F:/题库输入表格/CIE P3 录入/24_FM_32.xlsx`（11 题，结构化模板；仅 Q7 有 DISPIMG 配图 → 提取 `xl/media/image1.png` 存 `data/images/24_FM_32_7.png`）。
- 该文件题干已是真实 Alt+Enter 换行（REAL_NL），**无字面** `\newline`/`\n\n`，符合新约定，录入即正确。
- 章节按需判定：Q1 多项式除法→Algebra(factor/remainder)；Q2 二项展开→Algebra(partial fractions/binomial)；Q3/Q5 复数→Complex numbers；Q4 对数→Log&exp；Q6 隐函数求导→`"Algebra"`(预设无 Differentiation)；Q7 迭代求α→Numerical solution of equations；Q8 三角 Rsin→Trigonometry；Q9 向量→Vectors；Q10 部分分式+积分→Integration；Q11 微分方程→Differential equations。难度 2–4。
- 安全注入（行式插入 `];` 前，保留 Store）+ 强制校验：node --check OK；Store 计数=1 未截断；题数 214→**225**（24FM32=11）；每题均有 board（无稀疏空洞）。
- ⚠️ 修正 MEMORY.md 月份误记：`ON` 实际用 `"Oct/Nov"`（非 "Oct/Nav"），以库内数据为准。
- 待办：本批次与 22_MJ_31 一并 commit+push（含 22_MJ_31_9/10.png 之前漏提交）。

## CIE P3 新增 `Differentiation` 章节 + 重归类（21:1x）
- 用户要求 P3 新增题目分类 `Differentiation`，隐函数求导 / 参数方程求导 / 求 maximum,minimum（含驻点）均归属。
- 在 `CHAPTER_PRESETS` 的 CIE|P3 数组加入 `"Differentiation"`（现 11 章）。
- 重归类 21 道 cie_p3 题：纯微分法(隐式/参数/极值)→`["Differentiation"]` 共 13；极值+面积/体积大题→`["Differentiation","Integration"]` 共 4（25ON33_q10/25MJ31_q11/24ON31_q6/24ON32_q11）；迭代定位驻点→`["Differentiation","Numerical solution of equations"]` 共 4（25ON33_q8/25FM32_q7/22MJ31_q10/24FM32_q7）。
- 边界判定：微分方程求解仍归 Differential equations；复平面 |z| 极值归 Complex numbers（25MJ35_q5）；求给定梯度后积分归 Integration（25MJ35_q7）；模函数/对数题保持原章。
- 强制校验：node --check OK；Store 计数=1 未截断；脚本自检「含微积分类 token 但未重归类」恰好都是 DE/Complex/Integration/模/对数等合理留存项。
- 脚本：`reassign_diff.py`（含自检）；分类依据 dump：`classify_diff.py`/`dump_calc.py`。

## CIE P3 2024 MJ Paper 33 录入（23:1x）— 11 题
- 用户提交 `F:/题库输入表格/CIE P3 录入/24_MJ_33.xlsx`（11 题，结构化模板；仅 Q4/Q9 有 DISPIMG 配图）。
- 配图映射经 cellimages.xml+rels 精确解析：Q4→`xl/media/image2.png`、Q9→`xl/media/image1.png`（xlsx 内 DISPIMG ID 与 media 顺序不对应，不能按序假设）。已提取为 `data/images/24_MJ_33_4.png`、`24_MJ_33_9.png`。
- 题干全部真实 Alt+Enter 换行，无字面 `\newline`/`\n\n`，符合新约定。
- 章节判定（用新增 Differentiation 章）：Q2 驻点→Differentiation；其余按内容：Q1/Q4 对数指数、Q3/Q6 复数、Q5 部分分式、Q7 因式定理、Q8 三角 Rcos、Q9 微分方程、Q10 向量、Q11 积分。难度 2–4。
- 安全注入（行式插入 `];` 前，保留 Store）+ 强制校验：node --check OK；Store 计数=1；cie_p3 总数 119→**130**（24MJ33=11）；两配图文件已落盘。

## CIE P3 2024 MJ Paper 32 录入（23:2x）— 10 题
- 用户提交 `F:/题库输入表格/CIE P3 录入/24_MJ_32.xlsx`（10 题；仅 Q6 有 DISPIMG 配图）。
- 配图映射：Q6 DISPIMG `ID_6FB40FFE...` → rId1 → `xl/media/image1.png` → `data/images/24_MJ_32_6.png`（经 cellimages.xml+rels 精确解析）。
- 章节判定：Q1 |x-2a| 图+不等式 → `Algebra (modulus functions)`（预设恰有此章）；Q4 隐式求导求梯度 → `Differentiation`；Q6 极大值+积分 → `["Differentiation","Integration"]`；Q10 sec^3 求导步+解 DE → 整体归 `Differential equations`（解微分方程题保持原章，不转 Differentiation）。难度 2–4。
- 安全注入 + 强制校验：node --check OK；Store 计数=1；cie_p3 总数 130→**140**（24MJ32=10）；无字面转义残留。
